[project]
name = "saihai-backend"
version = "0.1.0"
description = "SaihAI backend (FastAPI) - mock tactical simulator"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.115.0",
  "psycopg[binary]>=3.1.19",
  "sqlalchemy>=2.0.36",
  "uvicorn[standard]>=0.30.0",
]
